apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bootstrap.fullname" . }}-test-connection"
  labels:
    {{- include "bootstrap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bootstrap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
